{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "CreateAdminPageComponent",
  "title": "create admin page component",
  "properties": {
    "name": {
      "type": "string",
      "description": "name of component",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the component",
      "default": "job-title"
    }
  },
  "required": [
    "name"
  ]
}
