{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "CreateCrudService",
  "title": "create scaffold for Admin CrudService",
  "properties": {
    "name": {
      "type": "string",
      "description": "service name",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "default": "jobTitle",
      "x-prompt": "Please enter service name to be generated"
    },
    "debugMode": {
      "type": "boolean",
      "description": "while test it on the local files please make it true",
      "default": false
    }
  },
  "required": [
    "name"
  ]
}
