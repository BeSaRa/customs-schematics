{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "CreateRoute",
  "title": "Update the administration Routing module file",
  "properties": {
    "name": {
      "type": "string",
      "description": "add localization to lang-keys-contract but with prefix menu",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "with is the localization key",
      "default": "job-title"
    },
    "debugMode": {
      "type": "boolean",
      "description": "while test it on the local files please make it true",
      "default": false
    }
  },
  "required": [
    "name"
  ]
}
